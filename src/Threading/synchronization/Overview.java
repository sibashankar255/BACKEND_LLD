package Threading.synchronization;

public class Overview {


    //conditions leading to sync problem:

    //1.critical section:-
    //part of code working on same data/value

    //2.race section:-
    //when more than 1 thread enters critical section at the same time
    //if we can prevent more than 1 thread to enter into critical section

    //3.preemption(pause):-
    //if a thread(t1) in critical section is preempted by OS, to do something else
    //if thread has entered C.S -> no preemption allowed

    //properties of good solution:-

    //1.offer mutual exclusion:-
    //




}
