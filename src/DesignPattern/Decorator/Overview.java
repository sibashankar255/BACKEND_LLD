package DesignPattern.Decorator;

public class Overview {
    /*
    provides wrapper to existing class which allows to have added functionality.

    decorator dp:
    case where we add properties to base entity at runtime:-> final output depends on base
     */
}
