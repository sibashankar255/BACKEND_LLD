package DesignPattern.PrototypeRegistry;

public class Overview  {

    /*
    problem:-
    given an object , we need to create a copy of the same


    whenever we want to create multiple objects of same type+
    creating them is heavy operation

    get a copy of already created object


    1.create same object again and again
    2.get copy of already created object + append data



    REGISTRY:-
    generally a class which has mapping for objects to tag(name)

     */
}
